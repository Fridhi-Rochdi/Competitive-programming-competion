#include <bits/stdc++.h>
using namespace std;
void solve(){
    int a, b;
    cin >> a >> b;
    cout << fixed << setprecision(1) << 0.5 * a * b << endl;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int T;
    cin >> T;
    
    while (T--) {
     solve();
    }
    
    return 0;
}
